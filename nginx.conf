# nginx.conf
server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Critical for React/Vite single page applications
        try_files $uri $uri/ /index.html;
    }
}